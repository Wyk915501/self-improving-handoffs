冻结

# 设计要点与审查史（2026-09-08 → 09-09）

**这份回答什么**：这套东西为什么长成这样、审查方各轮挑过什么、哪些是有意不做的。不含原项目的任何文件与路径；时间是北京时间。

## 一、设计要点（每条都对应一次被挑出来的错）

1. **模型没有文件写权。** 每日学习里模型只返回候选 JSON（`claude -p --tools ""`），入表、编号、到期、写更新记录全由程序做。理由：一旦模型能写表，"程序核验"就没有意义。
2. **证据要能机检。** 每条证据的引文必须逐字见于该判决件原文；≥2 条证据来自 ≥2 个不同原始事件，且引用的原件必须在树里实存。理由：审查方第一轮就指出"存在 report_id 不等于有证据"。
3. **否决是一个脚本永不写的文件。** 表本身脚本会重写，负责人改表的那一刻有被冲掉的时间窗；单独一个只读文件，读取优先，就没有竞争。已生效的行进否决记录同样撤回。
4. **48 小时否决窗，不设总闸。** 负责人明确接受"坏规矩可能自动生效、事后否决"这个残余风险，换取不用逐条审批。到期前 6 小时看门狗再催一次。
5. **生效版带凭证。** 生效版文件头嵌表的哈希前 8 位；publish 写后自检，看门狗每轮再验，不一致自动重生成。理由：审查方实测过表改了生效版没跟上。
6. **每台机器只写自己那份清单。** docs 树两端双向同步时，两端写同一路径会互相覆盖；待处理清单按机器分文件名。发布者只能有一台。
7. **检查器只查格式，不查内容；只报确定性错误。** G6 只证明本机存在；G8 只报三类零误报项，跨机与归档断链有意不报。宁可漏报，不制造让人麻木的噪音。
8. **失败要可见。** `--todo` 写不出退出 2，不假报成功；缺 PyYAML 时 fail-closed；测试全走临时目录环境变量，不碰真实家目录。

## 二、审查史（谁挑了什么、改了什么）

| 轮 | 审查方 | 挑出的主要问题 | 处置 |
|---|---|---|---|
| 09-08 R1 | Codex（代码复核） | 模型自报类别当安全依据；`report_id` 存在就算证据；otherwise 三处 P1 | 改成引文逐字核验＋独立事件；建三份回归测试 |
| 09-08 R2 | Codex（自审重整） | "接通／运转／有用"混在一起说；规则没有修订／停用链路；通知送出 ≠ 人看过 | 状态表按三档分列；修订链路列为未做；边界如实写 |
| 09-09 R1–R3 | 远程机 Claude（复现） | 测试偷偷写真实家目录；生效版表哈希与表脱钩；表被手改没登记 | `HANDOFF_TOOLS_DIR` 隔离；publish 写后自检＋看门狗自愈；前向补登 |
| 09-09 | Codex（部署） | 发布者与消费者不能双写同一路径；服务器不装模型与弹窗 | 单写版：远程机只装 hook＋零模型 cron，每机各写各的清单 |
| 09-09 | 负责人 | 弹窗只说"某某不合格"看不到原因；几个按钮开同一页没意义；"我要的是一个自动的东西，只有它出了问题才需要我介入" | 两档弹窗（需要你介入／仅告知）；单按钮开状态页；扫描结果不弹给人、写进清单让 AI 自查 |
| 09-09 | 远程机 Claude | 三份索引把链接写成 `目录\文件`；两份文件名含反斜杠 | 新增 G0（文件名）与 G8（索引链接） |
| 09-09 | Codex ＋ Astra（对抗） | G8 把合法 Markdown 标点转义与 `~~~` 围栏当真链接；看门狗先收满 20 个报告会饿死索引检查 | v2.3：围栏状态机、转义解释；收集阶段不设 cap |
| 09-09 | GLM-5.3（对抗，两轮） | G8 漏正斜杠目录前缀；NTFS 大小写误判；`#` 文件名被当锚点；hook 写索引拦不住；G6 反斜杠本机能解析就放过；`feedback_for` 可编造；改过的件不再读；越界关键词可改写绕过、"一律不进表"是夸大；15 条后静默截断；看门狗自指盲区 | v2.4／v3.2／v1.3 全部改掉；一项反驳（手工否决行不占上限，代码已排除，GLM 二轮撤回） |
| 09-09 | GLM-5.3（二轮，收口确认） | 上一轮 9 项中 4 项没堵住：反斜杠链接项与断链同编号、被扫描一起滤掉；publish 文档说只警告、代码仍截断；旧格式去重条目让改过的件仍永久跳过；原件实存核验只堵"编造 ID"不堵"编造事"。另 5 项新引入：`#` 只修了索引没修报告、载荷深搜被正文里的别件路径劫持、目录前缀类用不分大小写的 exists、目录列不出拿空集继续判、升级文案与常驻档矛盾；越界词表误拒合法教训 | v2.5／v3.3／v1.4 全部改掉；"程序守不住"从一条写成两条 |

## 三、有意不做的

- 不做 sha 级证据链、三轨信号、试点合同、逐拍收据——原设计有，两天一拍没跑成，负责人否定。
- 不做规则修订／替换链路——现在只会追加与否决，15 条满后新想法只进观察。
- 不做离机告警——弹窗只在登录桌面可见。
- 不让新规矩自动变成程序检查项——规矩给 AI 读，检查靠代码，两条线分开。
- 不让检查器拦住写入——它是写后提示，不是闸门。

## 第 6 版（2026-09-21）：两轮独立核查后的加固

发布后两天里，原项目把这套东西当成自己的基础设施日常使用，暴露出四个真问题，由对面机器的独立审查（两个独立上下文的 fable 子代理，只给 diff 与快照不给推理）逐个复现、逐个修掉：

1. **采纳入口没有鉴权**：任何本机程序都能调命令把候选写成规矩。修法不是两步确认（多点一次，防不了恶意程序），而是**处理页口令**——页面「采纳」链接里带一个由本机种子＋候选全文算出的 10 位短码，命令行不带口令一律拒。防网页、误触、记着旧编号的脚本；防不了能读本机状态文件的恶意程序，那层仍靠 48 小时否决窗。
2. **截断卡死提醒**：规范扫描显示只列 20 条，"发现变多才提醒"的判定取自截断后的输出——超过 20 条后新增 12 次漏报 8 次（实测复现）。改成每条发现带稳定键（类别＋路径，不含天天变的天数），按"出现新发现"提醒，且新指纹只在提醒**成功弹出后**才进基线。
3. **状态竞态**：看门狗运行期间负责人点的「同意」会被结尾的整份状态写回抹掉。改 merge 写回（磁盘优先）；采纳写表改走每日学习的锁＋哈希门（原来能覆盖并发写入并撞号）。
4. **坏池丢历史**：候选池文件读不出来时原样当空池继续，下次保存即清空历史。改成先读完关句柄再解析，只有内容损坏才改名留存（文件名到微秒），一时读不到本轮不碰池。

同轮补齐**克隆-调整-归档**路线的全部配套：已归档旧件的旧链接由写后检查放行（原址不留跳转件——跳转件自己过不了检查还会被数进碎片化）；归档件不进碎片化计数、不送每日学习、不当"原始事件"；流程规范定成三档制（旧报告仍成立→只改正本；有话不成立→克隆；结论变→更正件）。测试 159→205 项，其中新增用例逐条做过"把修复改回去必挂"的变异自查；对面机器另派独立子代理做了 23 个变异的对抗式验收。

这一版的方法论收获：**"会发提醒或能写表的改动，等别的工具看过再写已修"**——本轮两条最深的 bug（截断卡提醒、Windows 句柄）都是写者自己测完觉得没问题的地方。
