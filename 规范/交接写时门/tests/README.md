现役

# 交接写时门 · 回归测试

**为什么存在**：Codex 的 [reproduce.py](../../../工作传递/自我改进循环/codex/2026-09-08_交接写时门复核/reproduce.py)、[check_*_v2.py](../../../工作传递/自我改进循环/codex/2026-09-08_v2合并复核/README.md)、[check_boundaries.py](../../../工作传递/自我改进循环/codex/2026-09-08_规则连接复核/README.md) 是"缺陷复现"脚本，锁定被审版本的 SHA，脚本一改就拒跑；它们是历史证据，不是正确性测试。这个目录放**正确行为**的回归测试，脚本每改一版都要跑一遍。

**谁该看**：改 handoff_gate.py／handoff_lessons.py／handoff_notify.py 的人；复核的人（Codex／US3）。

**什么时候看**：改脚本之后、交出之前。

## 怎么跑

```
py -X utf8 docs/规范/交接写时门/tests/test_gate.py       # 检查器 53 项
py -X utf8 docs/规范/交接写时门/tests/test_lessons.py    # 每日学习 35 项（假模型，不调 claude／不联网）
py -X utf8 docs/规范/交接写时门/tests/test_notify.py     # 看门狗 39 项（不弹窗、不起 publish 子进程、不碰注册表、不读真机计划任务）
python3 …                                                 # US3 同样三条
```

**隔离承诺（09-09 起属实）**：看门狗那份另设 `HANDOFF_NO_REGISTER=1`，不写 AppUserModelID 与 `handoff-rule:` 协议两处注册表（按钮逻辑照常走）。看门狗那份还把 `task_last_result` 换成假的（否则会读到真机上 HandoffDaily 的失败状态，早先的用例平白多出一条提醒）。三份测试开头都把 `HANDOFF_TOOLS_DIR` 指到自己的临时目录，脚本的状态文件、锁、限流戳、日志、状态页全落在那里；不碰真实 `~/.claude/tools/`、不碰 docs、不发网络请求、不弹窗。US3 Claude 1550 R1 指出 09-08 版并非如此（曾写并删真实的 `handoff_gate_lastscan`），已修。退出 0 = 全过。

## 覆盖了什么

- **test_gate.py**：带引号 status／profile、非法 YAML 无连带噪声、裸与带引号的重复键、坏日期、observed 晚于 frozen、链接五种写法与代码块忽略、G7 仅 hook 模式且刚冻结的好件静默、裸相对文件名、范围外／混合／目录不存在的退出码、非目标文件静默、异构载荷深搜、`--hook-strict`、兜底扫描限流／分批／收敛、**状态文件在临时目录、`--todo` 任意文件名与 `--writer`、目标不可写退出 2 且不说"已写"、G0 反斜杠文件名**、**G8 索引链接（真实路径反斜杠与本机当前缺同目录目标会报；合法 CommonMark 标点转义、锚点转义、波浪线围栏、`../`／`/root/…`／http／子目录／行内代码不报；`_archive/` 不在范围；`--scan` 进清单标「索引」且不套 G1–G4；显式指定只做 G8）**、**v2.4：③正斜杠目录前缀被报、`.MD` 大小写不同视为不存在、文件名含 `#` 整体存在即过、hook 写索引 README 当场只报①③而「尚未落盘」静默、G6P 反斜杠链接判不可移植**、**v2.5：`--scan --todo` 滤 G6 留 G6P、G6 目标文件名含 `#` 整体存在即过／剥锚点仍不存在才报、hook 载荷正文里的别件绝对路径不劫持、③ `Sub/` 与 `sub/` 逐段精确**。
- **test_lessons.py**：正常批只收合格候选、虚构引文被点名、越权规则被拦、单事件身份不够、同批近似去重、立即重跑无新件、日配额从表推导、预算积压三轮清空、否决记录优先且近似候选不进表、状态文件写失败时表仍算提交且重跑不重复加行、已生效行被否决记录点名即同步为否决、生效版扣除否决与拟生效且不含引文、锁占用时跳过、**v3.2：`feedback_for` 指向不存在的原件不算独立事件、processed 带 `@改动时刻`、"移出…目录"类改写被越界关键词层拦下、**v3.3：旧裸 `report_id` 不再永久跳过、16 条生效全发不截断、判决件互引不算原始事件**、**v3.5：SSE 行流拼接与 usage 提取、坏行不炸、缺 `GLM_API_KEY` 明确报错、`--provider` 分流（glm 不碰 claude CLI 且默认 sonnet 不会被当成 GLM 模型名）**。
- **test_notify.py**：从未成功的 36 小时宽限（首次发现记时、35 小时不弹、37 小时弹、同日不重复）、成功后清零、上次成功后 37 小时再弹、定时任务不存在不评估、故障类常驻弹窗且唯一按钮「看处理」、**页面与状态都在临时目录**、**`--with-scan` 覆盖索引 README（G8）且 `_archive` 与跨机链接不报；20 个坏报告不会占满旧 cap 后饿死索引**、**v1.3：拟生效到期前 3 小时升为需要介入且常驻、同一到期只升一次、**v1.4：`scan_problems` 留 G6P 滤 G6**、**v1.5：倒计时说人话（32 分钟不再是 0 小时）、12 小时升级窗、已过期未翻牌仍提醒且无负数倒计时；**弹窗对人可读的契约**——标题正文里不许出现 `LG-` 编号、不许出现"否决/拟生效/每日学习/否决记录"这些内部词、不塞规矩正文、只留一个「去看看」按钮；**判断在页面上**——处理页有「等你拍板」区、两条规矩全文、每条两个按钮且 href 指向正确编号、解释了规矩哪来的与不做事会怎样；拍板命令（no 写进表格并补更新记录／重复点不重写／表里没有的不写／动作或编号非法一律忽略／ok 只记状态不碰 docs）；**计划任务上次结果码非 0 当场报（并点破 0xC000013A 是窗口被关掉杀的）、同一次失败只报一次、结果码 0 与"从没跑过"不报**。

## 更新记录

- 2026-09-10 16:3x：按负责人反馈加 notify 21 项（倒计时、对人可读契约、页面拍板区、过期段、拍板命令 7 项）。计数 53+35+39=127。（claude-code）
- 2026-09-09 20:3x：三轮加 lessons 1 项（判决件互引）。计数 53+30+14=97。（claude-code）
- 2026-09-09 20:2x：按 GLM 二轮加 gate 5 项、lessons 2 项、notify 1 项。计数 53+29+14=96，两机全过。（claude-code）
- 2026-09-09 20:0x：按 GLM 复核加 gate 8 项（G8③、大小写、`#`、hook 索引 ×2、尚未落盘静默、G6 反斜杠、上级/绝对/子目录不报）、lessons 3 项（幽灵原件、`@mtime`、改写越界）、notify 2 项（到期前升级、只升一次）。计数 48+27+13=88，两机全过。（claude-code）
- 2026-09-09 19:03：按 Astra 对抗复核补 G8 三个反例（合法标点转义、锚点转义、`~~~` 围栏）并修正；看门狗生产扫描去掉收集阶段默认 cap，补 20 个坏报告 + 1 个坏索引不饥饿用例。计数 40+24+11=75。（codex + gpt-6-astra 只读对抗复核）
- 2026-09-09 18:55：gate 加 7 项（G8 索引链接：报两类、四类不报、_archive 排除、正常不报、进清单标索引、显式指定）；notify 加 1 项（扫描器覆盖索引）。计数 37+24+10=71，两机全过。（claude-code）
- 2026-09-09 17:40：按 US3 Claude 1550 R1 与 Codex 1557 §三.2：测试改走 `HANDOFF_TOOLS_DIR` 临时目录（隔离承诺从此属实，US3 上跑完 `/root/.claude/tools/` 无 handoff_* 残留）；gate 加 5 项（隔离、`--todo/--writer`、不可写退出 2、G0 反斜杠 ×2）；notify 加 1 项。计数 30+24+9=63，两机全过。（claude-code）
- 2026-09-08 22:55：加 test_notify.py；test_lessons 加 G/H 两组（否决同步、生效版、锁）。
- 2026-09-08 21:45：建目录，两份测试随 v2.1／v3 一起交。
