## 协作教训（写交接报告前照做；自动维护）

@<相对于本 CLAUDE.md 的路径>/工作传递/协作教训-生效.md

上面这份由脚本每天生成，只含当前生效的规矩（≤15 条），别手改。带出处的完整表在 协作教训.md；要作废一条，在 协作教训-否决记录.md 加一行。写 `工作传递/**/*_交接报告.md` 时会自动做写后检查，不合格会当场把原因回给你，自己改。

### 状态词规则的例外
交接报告第 1 行必须是 frontmatter 的 `---`，状态写在 `status:`（draft / ready_for_review），不要在正文第一行写别的状态词——会把整个表头顶掉。
