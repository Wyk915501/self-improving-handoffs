- 写交接报告前先读 `<DOCS>/工作传递/协作教训-生效.md`（脚本生成的生效版，只有编号和规矩；完整表在 协作教训.md，否决写 协作教训-否决记录.md）。
- 开工先看**这台机器**的待处理清单（`<DOCS>/工作传递/写后检查-待处理-<本机名>.md`，每台机器只写自己那份）里有没有自己来源目录的条目：draft 就地改；已冻结的不回改内容。它只是近几小时的收件箱，不是长期待办。
